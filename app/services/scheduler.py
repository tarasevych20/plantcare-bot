from datetime import datetime, timezone

# Заглушка під нагадування: тут буде логіка вибірки користувачів,
# у яких настав час сповіщати (day-of чи pre-day), і надсилання повідомлень.

from ..bot import bot

async def tick_scheduler():
    # TODO: обрати користувачів і відправити їм повідомлення
    # приклад:
    # await bot.send_message(chat_id=user_id, text="Сьогодні догляд 🌿 ...")
    return
